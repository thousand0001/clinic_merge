# AGENTS.md

本專案是診所會員名單、疾病分流、檢驗資料、預防保健資料、分析表統計的 Excel 自動化工具。

請 Codex / AI coding agent 修改本專案時遵守以下規則。

---

# 零、快速決策規則

開始工作前依下列順序判斷，避免重複讀檔、重複驗證與不必要修改：

1. 先確認指定診所、指定程式與來源資料夾；不要掃描無關診所檔案。
2. 只執行正式 Excel 時，直接使用既有對應程式；需要改碼時才建立新日期版本。
3. 單一診所差異放在診所專用前置清洗或後處理，不修改 `選會員_共用核心_*.py`。
4. 來源檔案只盤點一次，讀取時同步建立筆數、命中數、L/M/N/O、分析表等 counter。
5. 正式輸出後只讀必要 sheet 與必要欄位核對，不掃描整本 workbook。
6. 驗證失敗時先修正原因；禁止原封不動重跑相同慢速流程。
7. 已有可信 counter 或前一次命令結果時直接沿用，不為取得相同資訊再次讀檔。
8. 測試共用核心時不要使用書田資料；書田必須先走 `前置清洗_書田_*.py`。

---

# 一、基本原則

1. 不要破壞既有 Excel 樣板格式。
2. 不要任意改變既有欄位順序。
3. 不要任意刪除既有功能。
4. 優先做最小幅度修改，除非使用者明確要求重構。
5. Python 需相容 Python 3.9。
6. 輸出檔名需加時間戳，避免覆蓋舊檔。
7. 輸出檔預設存到輸入檔同資料夾或使用者指定資料夾。
8. 不要自動刪除原始檔案、樣板檔案或輸出檔案。
9. 中文訊息請使用繁體中文。

---

# 二、openpyxl 效能規則

處理大型 Excel / ODS / XLSX 時，不要用 openpyxl 完整載入整本 workbook 後才做統計。

禁止以下流程：

1. 產生輸出 Excel 後，再用 openpyxl `load_workbook()` 重新打開輸出檔統計。
2. 掃描輸出 workbook 的所有 sheet。
3. 掃描所有列、所有欄來統計「分析表」。
4. 對大型 xlsx 做完整 workbook 驗證。
5. openpyxl 卡住後反覆重跑同一個驗證流程。
6. 為整張工作表所有空白格套樣式。
7. 對整本 workbook 做無必要的深度格式複製。

正確做法：

1. 在資料讀取、分類、分流階段同步建立統計 counter。
2. 每筆資料分到哪個 sheet 時，立即統計備註代碼 0~5。
3. 最後直接把 counter 寫入「分析表」。
4. 不要再從輸出 workbook 回頭掃分頁統計。
5. openpyxl 主要用於寫入 Excel、保留格式、套用樣板，不要用來反覆掃描大型輸出檔。
6. 大型來源檔優先使用 `load_workbook(..., read_only=True, data_only=True)`。
7. 使用 `iter_rows(values_only=True)` 串流逐列處理，不要先 `list()` 載入整張工作表。
8. 同一來源檔只開啟一次；在同一次迴圈完成清洗、比對與 counter 累計。
9. 暫存處理只複製必要來源檔；除非流程確實需要，不要對大型來源資料夾直接 `copytree()`。
10. 能在記憶體中的標準化資料完成後處理時，不要為中間結果反覆寫檔再讀回。

---

# 三、分析表統計規則

分析表統計必須在資料分類階段完成。

建議使用：

```python
from collections import defaultdict, Counter

analysis_counter = defaultdict(Counter)
```

範例：

```python
sheet_name = "DM"
remark_code = "3"
analysis_counter[sheet_name][remark_code] += 1
```

常見 sheet：

```text
DM
CKD
DKD
ASCVD
CKD+ASCVD
DM+ASCVD
DKD+ASCVD
```

備註代碼通常是：

```text
0
1
2
3
4
5
```

最後直接把 `analysis_counter` 寫入「分析表」。

不要為了統計分析表而重新讀取輸出 workbook。

---

# 四、資料比對基本規則

身分證號比對前要正規化：

```python
def normalize_id(value):
    if value is None:
        return ""
    s = str(value).strip().upper()
    if s.endswith(".0"):
        s = s[:-2]
    s = s.replace(" ", "")
    return s
```

日期需支援：

```text
2026/03/20
2026-03-20
115/03/20
1150320
```

缺失值視為空：

```text
-
—
–
空白
None
NaN
```

不要用未正規化的 ID 直接比對。

## 身分證號／外來人口統一證號通用規則

本專案所有共用核心、前置清洗、診所專用程式與驗證工具，都必須使用相同的 ID 規則：

1. 本國國民身分證號：第一碼為英文字母，第二碼為 `1` 或 `2`，後接 8 碼數字。
2. 新式外來人口統一證號：第一碼為英文字母，第二碼為 `8` 或 `9`，後接 8 碼數字。
3. 110 年 1 月 1 日以前核發的舊式外來人口統一證號：前 2 碼為英文字母，後接 8 碼數字；第二碼只接受 `A`、`B`、`C`、`D`。
4. 舊式統一證號的第二碼性別規則：
   - `A`、`C`：男性。
   - `B`、`D`：女性。
5. 格式初步判斷至少必須接受：

```python
re.fullmatch(r"(?:[A-Z][1289]\d{8}|[A-Z][A-D]\d{8})", normalize_id(value))
```

6. 禁止只使用 `[A-Z][12]\d{8}` 判斷有效 ID；這會錯誤排除第二碼為 `8/9` 的新式統一證號，以及第二碼為 `A-D` 的舊式統一證號。
7. `A900226967` 這類新式統一證號，以及 `AA12345678`、`AB12345678`、`AC12345678`、`AD12345678` 這類舊式統一證號，格式上都應視為可用，不可因第二碼不是 `1/2` 而列入待補或略過。
8. 若流程有檢查碼驗證，必須同時支援本國身分證號、新式統一證號與舊式統一證號，不可只套用本國身分證檢查碼規則。
9. 合法的新式或舊式外來人口統一證號必須和本國身分證號一樣，可作為會員去重、來源重疊、自選／不選、費用次數及輸出分頁的主鍵。
10. 只有空白、格式確實不符、檢查碼驗證失敗，或同一識別資料對應多個 ID 時，才可列為待補；終端機與 GUI 必須明確顯示來源檔、列號、姓名、原 ID 與原因。

## 電話號碼正規化規則

電話號碼比對或輸出前，先移除空白、連字號、括號等非數字字元，再依序處理：

1. 9 碼且第一碼為 `9`（行動電話）時，前面補 `0`。
2. 9 碼且第一碼為 `2`（市話）時，前面補 `0`。
3. 其他 8 碼電話，前面補 `02`（市話）。
4. 其他長度不推測區碼，保留清理後的純數字。

## 會員要選／不選重疊規則

所有共用核心、前置清洗與診所專用程式都必須使用同一套檔名判定規則：

1. 檔名需先正規化：轉小寫，並移除空白、連字號、底線及括號。
2. 檔名需包含「會員」或「選」，才進入自選／不選來源判斷。
3. **不選會員優先判斷**：檔名包含 `115X`、`不選`、`不要會員` 或 `不要` 時，視為不選會員來源 `E2`。
4. 排除不選條件後，檔名包含 `自選`、`要選`、`預選` 或獨立年度標記 `115` 時，視為自選會員來源 `E1`。
5. `11501`、`11502` 等月份檔名不可只因含 `115` 就判為自選會員。
6. 同一檔名同時符合自選與不選關鍵字時，必須以不選會員 `E2` 優先。
7. 不可要求固定完整檔名；診所更改檔名前後文字時，只要符合上述關鍵字規則仍應正確辨識。

同一會員同時出現在「要選／自選會員」與「不選會員」時：

1. **實際選取結果以不選會員優先。**
2. 不可刪除或修改兩份原始來源名單。
3. `會員總表` 必須同時保留兩個來源旗標：
   - `是否為自選會員`：勾選。
   - `是否為115X`：勾選。
4. `會員總表` 的會員分類顯示 `E1/E2`，不可改成只有 `E1` 或只有 `E2`。
5. 重疊會員不可寫入最終「自選名單」輸出分頁；該分頁只保留純要選的 `E1`。
6. 核對時需分別回報純 `E1`、純 `E2`、重疊 `E1/E2` 筆數。
7. 重疊判斷優先使用正規化身分證號；來源沒有 ID 時，才使用正規化姓名搭配生日或電話。只用姓名時，必須確認姓名唯一。

---

# 五、Excel 格式規則

輸出 workbook 需盡量保留：

1. 樣板格式。
2. 合併儲存格。
3. 欄寬。
4. 列高。
5. 框線。
6. 底色。
7. 字型。
8. 對齊方式。
9. 分析表固定版面。

不要因為改效能而破壞樣板外觀。

不要把格式套到整張表、整欄、整列，避免檔案變大或卡住。

嚴禁為了補格線、邊框、字型或底色，對大量資料列逐格套用 openpyxl 樣式。這會讓輸出變慢、檔案變大，且容易卡住。

若需要維持外觀，優先使用以下方式：

1. 保留樣板既有格式。
2. 開啟工作表格線 `sheet_view.showGridLines = True`。
3. 只複製樣板中少量必要列的格式。
4. 只處理實際必要的小範圍儲存格。
5. 使用 Excel 表格樣式或條件格式，而不是逐格塗滿大量資料。

若資料列很多，寧可先輸出乾淨資料，不可為了外觀逐格套格式。

---

# 六、卡住程序處理規則

如果 openpyxl / Python 驗證程序卡住：

1. 可以使用 `ps` 查詢 PID。
2. 能精確確認是本次執行且已卡住的驗證程序時，可以直接停止，不需先詢問使用者。
3. 無法精確辨識程序時，必須先詢問使用者，不得猜測 PID。
4. 不要自行刪除檔案。
5. 不要停止不相關程序。
6. 停止後不要再重跑同一個慢速驗證。
7. 應改程式邏輯，避免再次卡住。

可使用：

```bash
ps -axo pid,command | grep "檔名關鍵字"
```

只有無法精確確認是本次驗證程序時，停止前才需讓使用者確認。

---

# 七、執行程式產生正式 Excel 規則

當使用者要求產生正式 Excel 時，可以直接執行本專案內對應的 Python 程式，不需再次詢問。

若需要修改程式碼，需先詢問使用者並取得同意；只產生 Excel 檔案則不用詢問。

允許執行的條件：

1. 執行的是本專案內的 Python 程式。
2. 目的是產生正式 Excel 輸出檔。
3. 不會刪除原始檔案。
4. 不會覆蓋既有輸出檔，檔名需加時間戳。
5. 不會執行慢速 openpyxl 完整 workbook 驗證。

執行完成後，只回報：

1. 是否成功。
2. 輸出檔路徑。
3. 是否有錯誤訊息。
4. `會員總表` L/M/N/O 欄位是否與輸入檔案加總匹配。

每次修改或產生正式選會員程式後，必須核對輸出檔 `會員總表` 的次數與費用欄位：

```text
L 欄：114 年次數合計
M 欄：114 年費用合計
N 欄：115 年次數合計
O 欄：115 年費用合計
```

核對規則：

1. 必須和輸入檔案或清洗階段統計出的加總比對。
2. 若輸入檔含明細列、小計列、合計列或總計列，必須排除重複層級，只取不重複明細或明確指定的統計層級。
3. 若輸出欄位是月平均欄位，不可拿來和總額直接比對；`會員總表` M/O 為費用總額，醫師看分頁 N/O 通常為月平均費用。
4. 比對結果若不一致，必須先找原因，不可直接交付。
5. 比對方式應採輕量核對：優先使用資料分類/清洗階段同步累計的 counter，或只讀取輸出檔必要 sheet 與必要欄位。

產生正式輸出檔後，除了核對 `會員總表` L/M/N/O，也必須用**清洗階段同步 counter** 檢查輸入資料是否有遺漏：

1. 來源資料夾的有效輸入檔案只盤點一次，後續沿用同一份清單。
2. 依來源類型檢查是否進入輸出或清洗後標準資料，例如：
   - 家醫/指定名單。
   - 自選會員。
   - 不要會員。
   - 門診次數與費用。
   - P4P 收案與追蹤。
   - 檢驗資料。
   - 預防保健/篩檢資料。
   - 最後就診日。
3. 對每一類來源資料，同步累計有效身分證筆數、輸出命中筆數、重要日期或金額/次數；不要為核對再次完整讀取來源檔。
4. 若某來源檔沒有進入輸出，必須明確標註原因，例如格式不支援、不是本次輸出需要的資料、無有效身分證、不是目標欄位來源、或需另做前置清洗。
5. 若發現來源資料應進入輸出但缺漏，必須先修正或向使用者說明，不可直接交付。
6. 輸出覆蓋檢查只讀必要 sheet 與必要欄位，例如 `會員總表` L:O、會員分類、來源旗標；不要掃描所有 sheet。
7. 要選／不選重疊時，額外核對 `E1`、`E2`、`E1/E2` 與最終自選名單筆數。

不要再重新打開大型輸出 workbook 做完整驗證；不要掃描整本 workbook、所有分頁或所有欄位。

## 正式輸出的最小驗證集合

除非使用者另有指定，正式輸出完成後只做以下檢查：

1. 程式是否成功完成及是否有錯誤訊息。
2. 輸出路徑與時間戳是否正確。
3. `會員總表` L/M/N/O 是否與清洗 counter 相符。
4. 各來源類型的清洗筆數與命中筆數是否相符。
5. 若有 115X，核對 `E1`、`E2`、`E1/E2` 與自選名單排除重疊筆數。

不得為「更放心」而追加完整 workbook 掃描、全分頁逐列統計或大量樣式檢查。

---

# 八、修改風格

修改程式時：

1. 優先保留原本函式名稱。
2. 優先新增小函式，不要大幅破壞架構。
3. 不要刪除使用者註解。
4. 不要改掉使用者指定的檔名規則。
5. 不要把繁體中文訊息改成簡體中文。
6. 不要把醫療欄位名稱翻成英文。
7. 不要改變 Excel sheet 名稱，除非使用者要求。
8. 若不確定，先做最小修改。
9. 每次修改程式後，必須檢查本次修改涉及的檔案，刪除已確認沒有用途的程式碼，避免程式持續膨脹。
10. 必須清理的內容包括：
    - 本次修改後不再使用的函式、變數、匯入與參數。
    - 已被新邏輯完整取代的舊分支、暫時 workaround、代理資料與重複處理。
    - 只為除錯或一次性驗證加入、正式流程不需要的程式碼。
11. 清理前必須先搜尋引用並確認不影響正式流程、相容流程與其他診所；用途不確定時不可刪除。
12. 此清理規則只適用於程式內無用程式碼，不可因此刪除歷史程式、備份檔、樣板、來源檔、正式輸出或使用者既有功能。
13. 清理後需執行與本次修改範圍相符的語法檢查或必要測試，確認沒有留下失效引用。

---

# 九、目前最重要的要求

## 共用核心保護

`選會員_共用核心_*.py` 已取代 `run_merge_通用_*.py`，是目前正式共用核心。執行時使用檔名日期最新版本；**使用者沒有明確要求修改時，一律不得修改**。

`run_merge_通用_0430_1.py` 是舊版相容程式，只供仍未轉接的舊前置清洗或歷史流程使用，不再作為新流程入口。不得刪除，也不要把新功能回寫到舊版。

若新樣板、新診所或單一需求需要調整，優先另開新的日期版本，或放在診所專用前置清洗/後處理程式，不得為單一診所需求直接修改共用核心。

## 共用核心直接執行診所

1. 耀聖（非新耀聖）：使用 `前置清洗_耀聖_*.py`（日期最新版本）。該程式負責讀取 `最後就診日.CSV`、`次數/` 月份 CSV，補齊身分證號後再呼叫共用核心。
   - 益民耀聖、周一珊耀聖已由 `前置清洗_景德_益民_恆御_一珊_*.py` 個別處理，不使用此前置清洗。
2. 展望（非展望雲端）目前使用 `前置清洗_展望_*.py`（日期最新版本）。該程式負責在不修改共用核心的前提下補展望專屬醫生看欄位，執行展望時不可再直接跑共用核心。
3. 耀聖雲端使用 `前置清洗_耀聖雲端_*.py`（日期最新版本）。目前用於愛鄰耀聖這類含 `費用/*.xlsx` 收入明細的資料夾，不可套用展望雲端或一般耀聖流程。
4. 新耀聖與展望雲端屬不同流程，仍使用各自專用程式，不可和上述診所混用。

## 共用核心測試範圍

1. 不使用書田原始資料直接測試 `選會員_共用核心_*.py`。
2. 書田有自己的 `前置清洗_書田_*.py`，執行或測試書田時必須先經該前置清洗流程。
3. 共用核心測試應使用已符合通用欄位格式的資料，避免把診所特有格式誤判為共用核心問題。
4. 只測試本次修改涉及的必要流程與必要欄位，不為測試共用核心掃描所有診所資料。

若任務涉及「分析表」統計，務必遵守：

```text
不要重新讀取輸出 workbook 掃描分頁。
不要用 openpyxl 對大型輸出檔做完整驗證。
分析表統計必須在資料分類階段同步累計。
最後直接把 counter 寫入分析表。
```

---

# 十、專案結構說明

```
clinic_merge/
├── AGENTS.md                                    ← 本規則檔；僅在使用者明確要求時修改
├── requirements.txt                             ← 套件清單，不可自動修改
├── 醫療群_衛福部資料.xlsx                        ← 參考資料，不可刪除
├── 選會員模板*.xlsx                             ← 輸出樣板，使用日期最新版本，不可修改格式
│
├── 選會員_共用核心_*.py                        ← 正式共用核心；使用日期最新版本
├── run_merge_通用_0430_1.py                    ← 舊版相容程式；不作為新流程入口
├── run_merge_周一珊_耀聖_0522.py                ← 歷史診所專用合併程式
├── run_merge_詹婦產科_展望雲端_0522.py          ← 展望雲端專用流程
│
├── 前置清洗_杏翔_0515.py                        ← 各診所前置清洗程式
├── 前置清洗_醫聖_0513.py
├── 前置清洗_汐止衛生所_0511.py
├── 前置清洗_書田_0430.py
├── 前置清洗_調和_0430.py
├── 前置清洗_藍主仕_0430.py
├── 前置清洗_方鼎_0430.py
├── 前置清洗_新耀聖_0430.py
├── 前置清洗_常誠_0430.py
├── 前置清洗_宏誠_0430.py
├── 前置清洗_南港復健_周宏法_0430.py
│
├── backup/                                      ← 備份資料夾；含選會員模板0430.xlsx，不可自動刪除內容
├── tests/                                       ← 測試資料夾
└── tools/                                       ← 工具資料夾
```

---

# 十一、檔案保護規則

1. `選會員模板*.xlsx` 是輸出樣板，**使用檔名日期最新版本**，不可覆蓋、不可修改格式。
   - 若明確需要 0430 樣板，路徑是 `backup/選會員模板0430.xlsx`。
   - 不要假設專案根目錄存在 `選會員模板0430.xlsx`。
2. `醫療群_衛福部資料.xlsx` 是參考資料，**不可刪除、不可覆蓋**。
3. `backup/` 資料夾內容**不可自動刪除**，且不納入 GitHub 上傳。
4. 修改需要建立新日期版本的 `.py` 程式時，應先由舊版複製出根目錄的新日期版本，後續只修改新版；不要先把舊版另外複製到 `backup/`，避免完成後又移動舊版而形成兩份備份。
5. 所有 `前置清洗_*.py` 是各診所專用，**修改時只改指定診所的檔案**，不要連帶修改其他診所。
6. 修改後的新 `前置清洗_*.py` 檔名後綴需更新為當天日期，例如 `前置清洗_杏翔_0523.py`。
   新版完成並通過必要檢查後，**舊版（前一日期）必須用 `mv -f` 移到 `backup/`，不可繼續留在根目錄；根目錄只保留最新版**。
   同一舊版只備份一次，不可同時使用 `cp` 與 `mv` 產生兩份相同備份。
7. `選會員_共用核心_*.py` 是正式共用核心，執行時使用日期最新版本；**除非使用者明確要求修改共用核心，否則不得修改**。
8. 修改共用核心時，必須建立當天日期後綴的新版本；新版完成並通過必要檢查後，將原版用 `mv -f` 移到 `backup/`，根目錄只保留最新版，不可同時留下新舊兩版。
9. `run_merge_通用_0430_1.py` 是舊版相容程式，不得刪除；除非使用者明確要求處理舊流程，否則不得修改。
10. 各診所特殊邏輯應優先放在對應 `前置清洗_*.py`，必要時在前置清洗呼叫共用核心後做診所專屬後處理，不要為單一診所需求改共用核心。
11. 若不確定要改哪支程式，**先詢問使用者**。

---

# 十二、程式分工說明

| 程式類型 | 說明 |
|----------|------|
| `前置清洗_*.py` | 讀取各診所原始資料，正規化欄位，輸出清洗後的中間檔 |
| `選會員_共用核心_*.py` | 正式共用核心；讀取標準資料並執行疾病分流、合併與 Excel 輸出，使用日期最新版本 |
| `run_merge_*.py` | 歷史或診所專用合併程式；依診所既有流程使用 |
| `run_merge_通用_0430_1.py` | 舊版相容程式，只供尚未轉接的舊流程使用，不作為新流程入口 |

修改流程建議：

1. 由根目錄舊版複製建立新的日期版本，只修改新版，不要先另外複製舊版到 `backup/`。
   新版完成並通過必要檢查後，用 `mv -f 舊版.py backup/` 移走舊版；根目錄只留最新版，舊版在 `backup/` 只保留一份。
2. 先改 `前置清洗_*.py`（資料清洗）
3. 確有共通需求時，才建立當天日期後綴的新 `選會員_共用核心_*.py`
4. 不要跨診所混用邏輯
5. 不要為單一診所需求修改共用核心；改用診所前置清洗或後處理承接差異。
6. 不要將新功能回寫到 `run_merge_通用_0430_1.py`。

---

# 十三、不確定時的預設行為

若規則有衝突，優先順序如下：

1. 保護原始資料與樣板格式（最優先）
2. 不刪除任何檔案
3. 做最小幅度修改
4. 先詢問使用者，再執行

---

# 十四、跨電腦移植與路徑規則

本專案完成測試後會移植到其他 Windows 或 macOS 電腦，程式不得綁定開發電腦。

1. 正式程式不得寫死任何特定電腦的絕對路徑，例如：
   - `C:\Users\某使用者\...`
   - `/Users/某使用者/...`
   - 特定 CloudStation、桌面、下載或 PyCharmProjects 路徑。
2. 來源資料夾必須由使用者透過資料夾選擇視窗選取，或由命令列參數明確傳入。
3. 使用者未傳入來源路徑時，應開啟 `tkinter.filedialog.askdirectory()`，不可退回使用開發電腦的固定路徑。
4. 輸出資料夾預設為使用者所選來源資料夾的同層或使用者指定位置，不得固定到開發電腦。
5. 樣板、共用程式與專案內資源應使用 `Path(__file__).resolve().parent` 建立相對於程式檔的位置。
6. 暫存資料應使用 `tempfile` 或系統暫存目錄，不得寫死 `/private/tmp` 或特定 Windows 暫存路徑。
7. 路徑處理統一使用 `pathlib.Path`，避免手動拼接 `/` 或 `\`。
8. 使用者取消資料夾選擇時，程式應顯示繁體中文提示並正常結束，不得繼續執行或產生 traceback。
9. 前置清洗整合進正式程式時，流程應為：
   - 使用者選擇該診所的實際來源資料夾。
   - 程式直接以所選資料夾執行前置清洗。
   - 清洗完成後接續正式 Excel 輸出。
   - 不得再用診所代碼或固定資料夾名稱拼接另一個來源路徑。
10. 交付或移植前必須搜尋程式中的絕對路徑、使用者名稱及開發電腦專屬目錄，確認正式執行流程不依賴這些路徑。
