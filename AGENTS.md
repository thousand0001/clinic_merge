# AGENTS.md

本專案是診所會員名單、疾病分流、檢驗資料、預防保健資料、分析表統計的 Excel 自動化工具。

請 Codex / AI coding agent 修改本專案時遵守以下規則。

---

# 一、基本原則

1. 不要破壞既有 Excel 樣板格式。
2. 不要任意改變既有欄位順序。
3. 不要任意刪除既有功能。
4. 優先做最小幅度修改，除非使用者明確要求重構。
5. Python 需相容 Python 3.9。
6. 輸出檔名需加時間戳，避免覆蓋舊檔。
7. 輸出檔預設存到輸入檔同資料夾或使用者指定資料夾。
8. 不要自動刪除原始檔案、樣板檔案或輸出檔案。
9. 中文訊息請使用繁體中文。

---

# 二、openpyxl 效能規則

處理大型 Excel / ODS / XLSX 時，不要用 openpyxl 完整載入整本 workbook 後才做統計。

禁止以下流程：

1. 產生輸出 Excel 後，再用 openpyxl `load_workbook()` 重新打開輸出檔統計。
2. 掃描輸出 workbook 的所有 sheet。
3. 掃描所有列、所有欄來統計「分析表」。
4. 對大型 xlsx 做完整 workbook 驗證。
5. openpyxl 卡住後反覆重跑同一個驗證流程。
6. 為整張工作表所有空白格套樣式。
7. 對整本 workbook 做無必要的深度格式複製。

正確做法：

1. 在資料讀取、分類、分流階段同步建立統計 counter。
2. 每筆資料分到哪個 sheet 時，立即統計備註代碼 0~5。
3. 最後直接把 counter 寫入「分析表」。
4. 不要再從輸出 workbook 回頭掃分頁統計。
5. openpyxl 主要用於寫入 Excel、保留格式、套用樣板，不要用來反覆掃描大型輸出檔。

---

# 三、分析表統計規則

分析表統計必須在資料分類階段完成。

建議使用：

```python
from collections import defaultdict, Counter

analysis_counter = defaultdict(Counter)
```

範例：

```python
sheet_name = "DM"
remark_code = "3"
analysis_counter[sheet_name][remark_code] += 1
```

常見 sheet：

```text
DM
CKD
DKD
ASCVD
CKD+ASCVD
DM+ASCVD
DKD+ASCVD
```

備註代碼通常是：

```text
0
1
2
3
4
5
```

最後直接把 `analysis_counter` 寫入「分析表」。

不要為了統計分析表而重新讀取輸出 workbook。

---

# 四、資料比對基本規則

身分證號比對前要正規化：

```python
def normalize_id(value):
    if value is None:
        return ""
    s = str(value).strip().upper()
    if s.endswith(".0"):
        s = s[:-2]
    s = s.replace(" ", "")
    return s
```

日期需支援：

```text
2026/03/20
2026-03-20
115/03/20
1150320
```

缺失值視為空：

```text
-
—
–
空白
None
NaN
```

不要用未正規化的 ID 直接比對。

---

# 五、Excel 格式規則

輸出 workbook 需盡量保留：

1. 樣板格式。
2. 合併儲存格。
3. 欄寬。
4. 列高。
5. 框線。
6. 底色。
7. 字型。
8. 對齊方式。
9. 分析表固定版面。

不要因為改效能而破壞樣板外觀。

不要把格式套到整張表、整欄、整列，避免檔案變大或卡住。

---

# 六、卡住程序處理規則

如果 openpyxl / Python 驗證程序卡住：

1. 先詢問使用者是否允許停止。
2. 可以使用 `ps` 查詢 PID。
3. 不要自行刪除檔案。
4. 不要停止不相關程序。
5. 停止後不要再重跑同一個慢速驗證。
6. 應改程式邏輯，避免再次卡住。

可使用：

```bash
ps -axo pid,command | grep "檔名關鍵字"
```

停止前需讓使用者確認。

---

# 七、執行程式產生正式 Excel 規則

當使用者要求產生正式 Excel 時，可以直接執行本專案內對應的 Python 程式，不需再次詢問。

若需要修改程式碼，需先詢問使用者並取得同意；只產生 Excel 檔案則不用詢問。

允許執行的條件：

1. 執行的是本專案內的 Python 程式。
2. 目的是產生正式 Excel 輸出檔。
3. 不會刪除原始檔案。
4. 不會覆蓋既有輸出檔，檔名需加時間戳。
5. 不會執行慢速 openpyxl 完整 workbook 驗證。

執行完成後，只回報：

1. 是否成功。
2. 輸出檔路徑。
3. 是否有錯誤訊息。
4. `會員總表` L/M/N/O 欄位是否與輸入檔案加總匹配。

每次修改或產生正式選會員程式後，必須核對輸出檔 `會員總表` 的次數與費用欄位：

```text
L 欄：114 年次數合計
M 欄：114 年費用合計
N 欄：115 年次數合計
O 欄：115 年費用合計
```

核對規則：

1. 必須和輸入檔案或清洗階段統計出的加總比對。
2. 若輸入檔含明細列、小計列、合計列或總計列，必須排除重複層級，只取不重複明細或明確指定的統計層級。
3. 若輸出欄位是月平均欄位，不可拿來和總額直接比對；`會員總表` M/O 為費用總額，醫師看分頁 N/O 通常為月平均費用。
4. 比對結果若不一致，必須先找原因，不可直接交付。
5. 比對方式應採輕量核對：優先使用資料分類/清洗階段同步累計的 counter，或只讀取輸出檔必要 sheet 與必要欄位。

不要再重新打開大型輸出 workbook 做完整驗證；不要掃描整本 workbook、所有分頁或所有欄位。

---

# 八、修改風格

修改程式時：

1. 優先保留原本函式名稱。
2. 優先新增小函式，不要大幅破壞架構。
3. 不要刪除使用者註解。
4. 不要改掉使用者指定的檔名規則。
5. 不要把繁體中文訊息改成簡體中文。
6. 不要把醫療欄位名稱翻成英文。
7. 不要改變 Excel sheet 名稱，除非使用者要求。
8. 若不確定，先做最小修改。

---

# 九、目前最重要的要求

若任務涉及「分析表」統計，務必遵守：

```text
不要重新讀取輸出 workbook 掃描分頁。
不要用 openpyxl 對大型輸出檔做完整驗證。
分析表統計必須在資料分類階段同步累計。
最後直接把 counter 寫入分析表。
```

---

# 十、專案結構說明

```
clinic_merge/
├── AGENTS.md                                    ← 本規則檔，不可修改
├── requirements.txt                             ← 套件清單，不可自動修改
├── 醫療群_衛福部資料.xlsx                        ← 參考資料，不可刪除
├── 選會員模板*.xlsx                             ← 輸出樣板，使用日期最新版本，不可修改格式
│
├── run_merge_周一珊_耀聖_0522.py                ← 診所專用合併主程式
├── run_merge_詹婦產科_展望雲端_0522.py
├── run_merge_通用_0430_1.py                    ← 通用合併主程式
│
├── 前置清洗_杏翔_0515.py                        ← 各診所前置清洗程式
├── 前置清洗_醫聖_0513.py
├── 前置清洗_汐止衛生所_0511.py
├── 前置清洗_書田_0430.py
├── 前置清洗_調和_0430.py
├── 前置清洗_藍主仕_0430.py
├── 前置清洗_方鼎_0430.py
├── 前置清洗_新耀聖_0430.py
├── 前置清洗_常誠_0430.py
├── 前置清洗_宏誠_0430.py
├── 前置清洗_南港復健_周宏法_0430.py
│
├── backup/                                      ← 備份資料夾，不可自動刪除內容
├── tests/                                       ← 測試資料夾
└── tools/                                       ← 工具資料夾
```

---

# 十一、檔案保護規則

1. `選會員模板*.xlsx` 是輸出樣板，**使用檔名日期最新版本**，不可覆蓋、不可修改格式。
2. `醫療群_衛福部資料.xlsx` 是參考資料，**不可刪除、不可覆蓋**。
3. `backup/` 資料夾內容**不可自動刪除**，且不納入 GitHub 上傳。
4. 修改任何 `.py` 程式前，需先把原 `.py` 檔複製到 `backup/` 資料夾保存。
5. 所有 `前置清洗_*.py` 是各診所專用，**修改時只改指定診所的檔案**，不要連帶修改其他診所。
6. 修改後的新 `前置清洗_*.py` 檔名後綴需更新為當天日期，例如 `前置清洗_杏翔_0523.py`。
7. `run_merge_通用_0430_1.py` 是共用核心程式，**除非使用者明確要求修改通用程式，否則不得修改**。
8. 各診所特殊邏輯應優先放在對應 `前置清洗_*.py`，必要時在前置清洗呼叫通用程式後做診所專屬後處理，不要為單一診所需求改通用程式。
9. 若不確定要改哪支程式，**先詢問使用者**。

---

# 十二、程式分工說明

| 程式類型 | 說明 |
|----------|------|
| `前置清洗_*.py` | 讀取各診所原始資料，正規化欄位，輸出清洗後的中間檔 |
| `run_merge_*.py` | 讀取清洗後資料，執行疾病分流、合併、寫入 Excel 輸出 |
| `run_merge_通用_0430_1.py` | 通用版合併程式，不針對特定診所；除非使用者明確要求，否則不可修改 |

修改流程建議：

1. 修改任何 `.py` 前，先將原 `.py` 複製到 `backup/`。
2. 先改 `前置清洗_*.py`（資料清洗）
3. 再改 `run_merge_*.py`（分流與輸出）
4. 不要跨診所混用邏輯
5. 不要為單一診所需求修改 `run_merge_通用_0430_1.py`；改用診所前置清洗或後處理承接差異。

---

# 十三、不確定時的預設行為

若規則有衝突，優先順序如下：

1. 保護原始資料與樣板格式（最優先）
2. 不刪除任何檔案
3. 做最小幅度修改
4. 先詢問使用者，再執行
