# 前置清洗程式摘要

更新日期：2026-06-25  
用途：整理本專案根目錄各 `前置清洗_*.py` 的適用診所、來源系統、清洗規則與注意事項。

## 維護規則

1. 之後只要新增、修改、改名、備份或停用任何 `前置清洗_*.py`，都必須同步 review 並更新本檔。
2. 修改正式流程時，先確認本檔中該程式的適用診所與規則，避免跨診所誤用。
3. 若某程式已被新版取代但仍留在根目錄，必須在本檔標示「歷史/舊版」或「不建議新執行」。
4. 本檔是摘要，不取代 `AGENTS.md` 的正式工程規則；若兩者衝突，以 `AGENTS.md` 的安全與版本規則為準。
5. 正式輸出仍需依 `AGENTS.md` 規則核對來源月份、`會員總表` L/M/N/O、`醫生看` K/L/M/N/O 與各來源命中數。

## 共通判斷

- 優先使用日期最新、且明確符合來源系統/診所的前置清洗程式。
- 一般新版流程應呼叫 `選會員_共用核心_*.py` 最新版；舊流程仍可能呼叫 `run_merge_通用_*.py`。
- 單一診所特殊規則應放在診所專用前置清洗或後處理，不應為單一診所直接修改共用核心。
- 若程式名稱涵蓋多診所，執行前必須確認來源資料夾名稱、檔案型態與程式內 clinic mapping。

## 程式摘要

| 程式 | 適用診所/來源 | 主要規則 | 核心/輸出入口 | 注意事項 |
|---|---|---|---|---|
| `前置清洗_耀聖_0623.py` | 一般耀聖系統；不含新耀聖、耀聖雲端、益民/周一珊等已拆專用流程 | 以姓名+生日建立 ID 對照；補 `最後就診日.CSV`、`次數/` 月份 CSV；支援 `預防保健名單.CSV` / `預防保健.CSV`；IC/檢驗代碼分流成健、BC肝、子抹、糞篩、老流；後處理補最後就診日與主次診斷 | 最新 `選會員_共用核心_*.py` | 若來源有新預防保健代碼，優先加在此程式；不可套用到新耀聖或雲端 |
| `前置清洗_健佑_0611.py` | 健佑診所，耀聖系統 | 耀聖規則的健佑專用版；補最後就診日、次數 CSV、預防保健名單；建立姓名+生日→ID | 最新 `選會員_共用核心_*.py` | 僅供健佑，不套用其他耀聖 |
| `前置清洗_新耀聖_0610.py` | 新耀聖系統 | 拆含「需照護名單」的雙工作表；避免百分位計算分頁誤判；轉換月份 xlsx/xls 成共用核心可讀格式 | 最新 `選會員_共用核心_*.py` | 新耀聖與一般耀聖/耀聖雲端不可混用 |
| `前置清洗_耀聖雲端_0618.py` | 耀聖雲端，例如愛鄰耀聖 | 保留根目錄 xlsx：自選會員、114 家醫名單、HealthCase、預防保健 cliScores；將 `費用/*.xlsx` 收入明細轉成月份費用；以病歷號優先對 ID，姓名唯一對 ID 為輔；未對到 ID 追加會員總表 | 最新 `選會員_共用核心_*.py` | 無正式次數檔時，用同一會員同一天算一次反推次數；若有正式次數檔應優先使用正式次數檔 |
| `前置清洗_展望_0618.py` | 一般展望系統，非展望雲端 | 直接呼叫最新共用核心；額外彙整 AW~BI 到醫生看；補算 AR/AS/BJ；支援展望新診所預防保健格式，把看診日期轉最後篩檢日期 | 最新 `選會員_共用核心_*.py` | AT 欄保留共用核心邏輯；BJ 由醫生看 BC=1 且 BG=1 判斷 |
| `前置清洗_品安安_0530.py` | 品安安展望雲端 | 使用展望雲端讀檔邏輯收斂資料；轉成通用標準來源資料夾；處理 X115、不選/自選、費用與醫生看後處理 | `run_merge_通用_*.py` + `run_merge_品安安_展望雲端_0525.py` | 百分位仍以通用規則為準；不修改原始資料 |
| `前置清洗_詹婦產科_0528.py` | 詹婦產科展望雲端 | 與品安安相近的展望雲端流程；讀 html/xlsx/xls；收斂成標準來源；處理費用、次數、預防保健、P4P/HealthCase | `run_merge_通用_*.py` + 品安安展望雲端讀檔模組 | 診所專用，避免與一般展望直接混用 |
| `前置清洗_台北公園諾貝爾_0528.py` | 台北公園諾貝爾展望雲端 | 展望讀檔邏輯收斂原始資料；轉通用標準來源；處理 R11440、次數、篩檢與醫生看後處理 | `run_merge_通用_*.py` | 百分位仍需有疾病樣態才納入糖心腎 KPI |
| `前置清洗_方鼎_0616.py` | 方鼎系統 | 保留方鼎 XLS 相容、月份檔名與姓名補 ID 規則；自選/不選交給最新共用核心 E1/E2 規則處理 | 最新 `選會員_共用核心_*.py`，方鼎 profile | 方鼎月份與姓名補 ID 規則不可拿去其他診所 |
| `前置清洗_調和_0609.py` | 調和系統 | 調和資料目前可直接交給共用核心；此程式主要清理 CSV 非法 XML 控制字元並作為模組化入口 | 最新 `選會員_共用核心_*.py` | 後續調和差異應往此程式收，不直接改共用核心 |
| `前置清洗_醫聖_0609.py` | 一般醫聖系統 | 若含 TXT 月份檔，先轉成通用 CSV；也會正規化 `醫聖月份費用xlsx`；其餘 Excel 型醫聖資料交給共用核心 | 最新 `選會員_共用核心_*.py` | 一般醫聖基底；特殊診所如思維另有 wrapper |
| `前置清洗_思維_0622.py` | 思維診所，醫聖系統 | 包裝最新醫聖前置清洗；修正思維 TXT 電話後多空白欄造成申請額位移，申請額取年齡欄後一欄 | 先套思維 patch，再呼叫醫聖前置清洗 | 僅供思維；不要把欄位位移邏輯回寫一般醫聖 |
| `前置清洗_南港復健_周宏法_0430.py` | 南港復健科醫聖、周宏法醫聖 | 將 `R11440次數/*.txt` 或 `次數費用/*.txt` 轉成通用 CSV | `run_merge_通用_*.py` | 舊式 TXT 轉檔流程；若要轉新共用核心需另評估 |
| `前置清洗_書田_0609.py` | 書田 | 書田 R11440 月分頁日期欄常是最後一次看診日；月份歸屬優先依分頁名稱 `11401`～`11512` 判定 | 最新 `選會員_共用核心_*.py`，ShutianProfile | 測共用核心不可直接用書田原始資料，須先走此程式 |
| `前置清洗_宏誠_0625.py` | 宏誠系統；目前已驗證德容宏誠 | 將 `費用/*.xlsx` 與 `次數/*.pdf` / `*.xlsx` 合成標準月份 CSV；身分證/日期/金額欄位模糊比對；民國日期轉西元；依實際 115 月份補正醫生看與會員總表月平均 | `run_merge_通用_*.py` | 德容案例需納入 11505；後續宏誠要確認 `醫生看` K/L/M/N/O 與實際月份一致 |
| `前置清洗_杏翔_0515.py` | 杏翔 | 將年度 `次數.xlsx` 轉通用月份 CSV；將 `次數/11401.xlsx` 類月次數檔轉 HISB 次數 CSV | `run_merge_通用_*.py` | 同時支援年度總表與逐月次數檔 |
| `前置清洗_景德_益民_恆御_一珊_0530.py` | 景德展望、益民耀聖、恆御復健科醫聖、周一珊耀聖 | 多診所合併專用：景德舊 R11440 .XLS；益民門診人次統計姓名補 ID/生日；恆御醫聖 TXT；周一珊耀聖 .XLS 月報；補必要空白表；輸出後核對 L/M/N/O | `run_merge_通用_*.py` | 益民/周一珊已不使用一般耀聖前置清洗 |
| `前置清洗_汐止衛生所_0511.py` | 汐止衛生所展望 | 將 `115自選會員.xlsx` 轉自選名單並帶生日；轉診斷代碼；將 114 年家醫會員就診次數年度總表轉 11401.csv；避開百分位分頁誤判 | `run_merge_通用_*.py` | 年度總表只代表指定統計層級，驗證時不可與逐月明細混算 |
| `前置清洗_藍主仕_0430.py` | 藍主仕 | 將 `主次代碼/*.CSV` 轉成通用月份 CSV | `run_merge_通用_*.py` | CP950 CSV；欄位以主次代碼來源為主 |
| `前置清洗_常誠_0430.py` | 常誠 | 將常誠主名單雙工作表拆成乾淨的 ascvd / 會員名單；移除百分位計算工作表干擾 | `run_merge_通用_*.py` | 避免百分位計算分頁誤判會員名單 |
| `前置清洗_賀自立_0618.py` | 賀自立耳鼻喉科診所 | 自選/不選依檔名分類，115X/不選優先；同 ID 重疊保留 E1/E2 並排除最終自選名單；來源可直接 ID，無 ID 時由同資料夾或對照檔建立姓名→ID；指定欄位 G/H/I/J/Q/O/P 對應會員總表 N/K/M/O/Q/R/T | 最新 `選會員_共用核心_*.py` | 目前賀自立最新版；115 年費用有效月份依最後看診日最大月份 |
| `前置清洗_賀自立_0615.py` | 賀自立耳鼻喉科診所歷史版 | 類似賀自立 0618，但舊欄位對應為 F/G/H/I/P/N/O | 最新 `選會員_共用核心_*.py` | 歷史/舊版；新執行優先用 0618，除非明確處理舊來源格式 |
| `前置清洗_友孝_0623.py` | 友孝診所；程式內也保留鄭骨科、大同 mapping | 內建資料整理；轉標準檔：家醫名單、自選/不選、門診次數費用、檢驗、P4P、篩檢；自動抓最新共用核心與模板 | 最新 `選會員_共用核心_*.py` + 最新模板 | 0623 已修成抓最新版核心/模板；實際執行需確認 clinic key |
| `前置清洗_鄭骨科_0605.py` | 鄭骨科耳鼻喉科；程式內也含友孝、大同 mapping | 舊版內建資料整理，與友孝同源 | 硬指定舊模板/舊核心引用 | 歷史/舊版，根目錄仍存在；新執行前需評估是否要升級成抓最新版 |

## 建議停用/清理觀察

- `前置清洗_賀自立_0615.py` 與 `前置清洗_賀自立_0618.py` 同時在根目錄；依目前規則新執行應優先 0618，0615 可視為歷史版。
- `前置清洗_鄭骨科_0605.py` 仍硬指定舊模板/舊核心風格；若再次使用，應先建立新日期版並改成抓最新版。
- 多診所共用程式如 `前置清洗_景德_益民_恆御_一珊_0530.py`、`前置清洗_友孝_0623.py`，修改時要避免為某一診所破壞其他 mapping。

