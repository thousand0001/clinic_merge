"""Sidecar unified preclean prototype for clinic_merge.

This package is intentionally read-only in its first stage: it inventories
source folders, detects known data shapes, and writes coverage reports without
changing the existing production preclean flows.
"""

